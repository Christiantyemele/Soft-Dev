# Plan: Main Entrypoint & Issue #30 Implementation

## Summary

Implement a proper main entrypoint for AgentFlow (replacing `real_test`) and complete issue #30 requirements for external collaborators.

## Issue #30 Requirements

From: https://github.com/The-AgenticFlow/AgentFlow/issues/30

**Current State:**
- Run via `cargo run --bin real_test` (confusing for external collaborators)

**Desired State:**
- `cargo build --release` produces `agentflow` binary
- `cargo install --path .` installs `agentflow` command
- Clean `.env` configuration

**Acceptance Criteria:**
- [ ] Main entry point that is NOT `real_test`
- [ ] `BUILD.md` documentation
- [ ] `RUN.md` documentation

## User Decisions

- **Package name**: Keep as `agent-team` (defer domain decision)
- **main.rs**: Rename to `agentflow-demo` binary

## Current Code Structure

| File | Purpose |
|------|---------|
| `binary/src/main.rs` | Demo with local node implementations (dry-run mode) |
| `binary/src/bin/real_test.rs` | **Production flow** - actual orchestration using real nodes |
| `binary/src/bin/demo.rs` | Mocked demonstration |
| `src/main.rs` | Outdated pocketflow-rs example (root crate) |

## Implementation Plan

### 1. Refactor Binary Structure

**Update `binary/Cargo.toml`:**
```toml
[[bin]]
name = "agentflow"
path = "src/bin/agentflow.rs"

[[bin]]
name = "agentflow-demo"
path = "src/main.rs"

[[bin]]
name = "demo"
path = "src/bin/demo.rs"
```

**File changes:**
- Move `binary/src/bin/real_test.rs` → `binary/src/bin/agentflow.rs`
- Keep `binary/src/main.rs` and add explicit `[[bin]]` entry as `agentflow-demo`
- Keep `binary/src/bin/demo.rs` as-is
- Keep package name as `agent-team`

### 2. Create BUILD.md

Document the build process for external collaborators:

```markdown
# Building AgentFlow from Source

## Prerequisites
- Rust 1.70+ (rustup recommended)
- Node.js 18+ (for GitHub MCP server)
- Claude Code CLI

## Build Commands
- Dev build: `cargo build`
- Release build: `cargo build --release`
- Install globally: `cargo install --path binary`
- Binary: `target/release/agentflow`

## Troubleshooting
- Common build errors and solutions
```

### 3. Create RUN.md

Document the runtime configuration:

```markdown
# Running AgentFlow

## Environment Setup
- Copy `.env.example` → `.env`
- Required keys: ANTHROPIC_API_KEY, GITHUB_PERSONAL_ACCESS_TOKEN
- Optional: GEMINI_API_KEY, OPENAI_API_KEY, GROQ_API_KEY (for proxy routing)

## Execution
- Direct: `./target/release/agentflow`
- Via cargo: `cargo run --bin agentflow`

## Configuration
- Redis backend (optional)
- LiteLLM proxy (optional)
```

### 4. Update Existing Documentation

**README.md:**
- Quick Start: `cargo run --bin real_test` → `cargo run --bin agentflow`
- Architecture section: update binary reference

**TUTORIAL.md:**
- All `real_test` references → `agentflow`

### 5. Cleanup

- Remove or archive `src/main.rs` in root (outdated pocketflow-rs example)
  - This is separate from `binary/src/main.rs`

## Files to Modify/Create

| Action | File |
|--------|------|
| Rename | `binary/src/bin/real_test.rs` → `binary/src/bin/agentflow.rs` |
| Edit | `binary/Cargo.toml` (add bin entries for all binaries) |
| Create | `BUILD.md` |
| Create | `RUN.md` |
| Edit | `README.md` (update commands) |
| Edit | `TUTORIAL.md` (update commands) |
| Delete | `src/main.rs` (root, outdated) |

## Execution Order

1. Delete `src/main.rs` (root - outdated pocketflow-rs example)
2. Rename `binary/src/bin/real_test.rs` → `binary/src/bin/agentflow.rs`
3. Update `binary/Cargo.toml` with explicit `[[bin]]` entries
4. Create `BUILD.md`
5. Create `RUN.md`
6. Update `README.md`
7. Update `TUTORIAL.md`
8. Verify: `cargo build --bin agentflow`
